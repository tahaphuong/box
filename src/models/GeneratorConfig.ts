/**
 * Model configuration for generating a random instance
 */
export interface GeneratorConfig {
  L: number;
  numRectangles: number;
  minWidth: number;
  maxWidth: number;
  minLength: number;
  maxLength: number;
}
